---
title: 吴恩达机器学习(一):监督学习
tags: 机器学习
categories: 机器学习
top_img: ../image/吴恩达机器学习(一)/cover.jpg
date: 2024-05-02 00:00:00
copyright: false
description: 吴恩达机器学习(一)
cover: ../image/吴恩达机器学习(一)/cover.jpg
---

**机器学习（Machine Learning）**，简称ML，专门研究计算机怎样模拟或实现人类的学习行为，以获取新的知识或技能，重新组织已有的知识结构使之不断改善自身的性能。

目前常用的机器学习算法有：
* 监督学习（supervised learning）
  * 回归（regression）
  * 分类（classification）
* 无监督学习（unsupervised learning）
  * 聚类（clustering）
  * 降维（dimensionality reduction）
  * 异常检测（Anomaly detection）
* 半监督学习（semi-supervised learning）
* 强化学习（reinforcement learning）


监督学习主要解决两类问题：回归（regression）和分类（classification）。

两类问题的主要区别在于：
回归问题的预测结果一般是连续的，而且对于无穷多个不同的输入，会对应无穷多个输出。比如最经典的房价预测问题，给定一个房子的参数，对房子的价格进行预测，不同面积的房子对应的价格也不尽相同。
![](/image/吴恩达机器学习(一)/regression.png)
分类问题的预测结果一般离散的，而且对于无穷多个不同的输入，对应的输出是有限个。比如肿瘤良性恶性的预测问题，对于不同的肿瘤大小，得到的结果只有良性或恶性两种结果。
![](/image/吴恩达机器学习(一)/classification.png)

# 一、回归
## 2.1 单变量线性回归
### 2.1.1 房价预测问题
我们先看最简单的房价预测问题：
我们已知一些房子的大小与价格，现在我们想找到一个函数$f(x)$使得当我们输入房子的大小$x$时，能够通过函数$f(x)$准确预测出当前房子的价格$\hat{y}$。
![](/image/吴恩达机器学习(一)/House_prices.png)
这里我们使用最简单的一次函数进行模型构建：$f_{w,b}(x)=wx+b$，其中$x$是输入的房子的大小，$w$和$b$是模型中的参数，不同的$w$和$b$对应了不同的直线，我们要做的就是通过调整$w$和$b$来使模型更好的拟合我们已知的数据（训练集）。
![](/image/吴恩达机器学习(一)/f(x).png)

### 2.1.2 代价函数
那么如何评价模型与训练集的拟合程度呢，这里我们引入代价函数（cost function）：平方误差成本函数（squared error cost function）进行评价：
$$J(w,b)={1\over2m}\displaystyle\sum_{i=1}^m({\hat{y}^{(i)}-y^{(i)}})^{2}$$
其中:$m$表示训练集中样本的数量，对于给定的样本输入$x^{(i)}$，$y^{(i)}$表示其实际结果，$\hat{y}^{(i)}$表示其通过模型得到的预测结果，其中
$$\hat{y}^{(i)}=f_{w,b}(x^{(i)})=wx+b$$
所以最终：
$$J(w,b)={1\over2m}\displaystyle\sum_{i=1}^m({f_{w,b}(x^{(i)})-y^{(i)}})^{2}$$
$J(w,b)$越小，说明我们的模型越拟合样本数据，所以我们的目标是使$J(w,b)$尽可能的小。

### 2.1.3 只考虑一个参数w
为了方便分析，我们先固定$b$的值为100，只考虑$w$对模型的影响：我们可以看到，当参数$b$固定时，$J(w,b)$就是一个关于$w$的开口朝上的二次函数，当$J(w,b)$最小时$w$在其对称轴处：
![w=110](/image/吴恩达机器学习(一)/w=110.png)
![w=200](/image/吴恩达机器学习(一)/w=200.png)
![w=280](/image/吴恩达机器学习(一)/w=280.png)

### 2.1.4 同时考虑w和b
当我们同时考虑$w$和$b$时，$J(w,b)$就是关于$w$和$b$的一个二元二次函数了，我们可以得到一个三维图像，将三维图像进行平面化后可以得到类似于等高线的一个图像，在等高线的中心处就对应了$J(w,b)$的最小值：
![](/image/吴恩达机器学习(一)/cost1.png)

## 2.2 梯度下降
### 2.2.1 算法思想
在上面我们通过调整$w$和$b$可以得到许多$J(w,b)$函数，我们想得到的是对于相同的样本输入，所得到的$J(w,b)$最小，那么如何得到想要的$J(w,b)$呢，这里我们就需要使用梯度下降算法。
梯度下降算法的思想如下：
* 设定$w$和$b$的初始值（比如$w=0,b=0$）
* 通过不断改变$w$和$b$的值来使$J(w,b)$减小
* 最终得到一个最小值或局部最小值时停止

我们可以在三维图上进行分析，假设我们初始时在山上某处，我们的目标就是要到山脚下，函数中某一点(x, y)的梯度代表函数在该点变化最快的方向。
![](/image/吴恩达机器学习(一)/gradient_descent.png)
不同的路径选择可能会使我们到达不同的局部最小值。

### 2.2.2 梯度下降公式
$w$和$b$的更新方法如下：
$$\begin{align*} \text{repeat}&\text{ until convergence:} \; \lbrace \newline
\;  w &= w -  \alpha \frac{\partial J(w,b)}{\partial w}   \; \newline 
 b &= b -  \alpha \frac{\partial J(w,b)}{\partial b}  \newline \rbrace
\end{align*}$$
其中：$$\alpha表示学习率，相当于下山时迈出步的大小$$
$$\frac{\partial J(w,b)}{\partial w}表示函数J(w,b)对w的偏导数，相当于下山时迈出步的方向$$
$$
\begin{align*}
\frac{\partial J(w,b)}{\partial w}  &= \frac{1}{m} \sum\limits_{i = 0}^{m-1} (f_{w,b}(x^{(i)}) - y^{(i)})x^{(i)} \\
\frac{\partial J(w,b)}{\partial b}  &= \frac{1}{m} \sum\limits_{i = 0}^{m-1} (f_{w,b}(x^{(i)}) - y^{(i)}) 
\end{align*}
$$
这里需要注意的是$w$和$b$必须同时更新：
![](/image/吴恩达机器学习(一)/gradient_descent_simultaneous.png)
如果按照上图右侧的方式进行更新，那么当更新$b$时使用的$w$就是更新后的$w$而不是原来的$w$了。

### 2.2.3 梯度下降的实现原理
![](/image/吴恩达机器学习(一)/realize.png)

在公式$w=w-\alpha\frac{\partial J(w,b)}{\partial w}$中，偏导数表示的是斜率，当$w$在最低点右侧时，斜率为正数，$w$减去一个整数则向左移动；当$w$在最低点左侧时，斜率为负数，$w$减去一个负数则向右移动。在移动过程中，由于$\alpha$不变，斜率逐渐变小，所以每次移动的步幅也会逐渐减小，最终不断收敛达到最低点；当达到最低点时偏导数为0，$w$就不再改变了。